(function(ctx) {
  'use strict;'
  ctx.data = {{stringify data}};
  ctx.pageSettings = {{stringify pageSettings}};
  ctx.settings = {{stringify settings}};
  ctx.firePageData = {{stringify firePageData}};
})(window.site.context);
